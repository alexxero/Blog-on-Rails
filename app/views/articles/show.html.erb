<h1><%= @article.title %></h1>

<h3>
  <%= @article.text %>
</h3>

<br>

<p>
  <%= time_ago_in_words(@article.created_at) %> by <b>  <%= image_tag(@user.avatar.url(:med)) %> <%= @article.author %> </b>
</p>

<h4 class="text-muted">Comments</h4>

<p>
  <%= form_for([@article, @article.comments.build]) do |f| %>

    <p>
      <%= f.label :body, class: "text-muted" %><br/>
      <%= f.text_area :body, class: "form-control" %>
    </p>
    <p>
      <%= f.submit 'Submit comment', class: "btn btn-success" %>
    </p>
  <% end %>
</p>

<% @article.comments.each do |comm| %>
    <p>
      <b>
        <%= comm.body %>
      </b>
    </p>
    <p>
      <i>
        <%= comm.author %>
      </i>
    </p>
    <p>
      <small>
        <%= comm.created_at %>
      </small>
    </p>
    <br>
<% end %>